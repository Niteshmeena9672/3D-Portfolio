// Import the custom component "SectionWrapper" from the "./SectionWrapper" file
import SectionWrapper from "./SectionWrapper";

// Export the "SectionWrapper" component to make it available for other modules
export { SectionWrapper };
